import React, { useEffect } from 'react';
import { Text, TouchableOpacity, View } from 'react-native';
import styles from './styles';

export default function AiScreen({ navigation, userData} ) {


  return (
    <View>
      <Text>AI Screen</Text>
    </View>
  );
}
